"""
Unit tests for the application.

Unit tests are fast, isolated tests that do not require external services.
They use mocks to simulate dependencies and focus on testing individual functions and classes.

Run unit tests only:
    uv run pytest test/unit/ -v

Run unit tests with coverage:
    uv run pytest test/unit/ --cov=src --cov-report=html
"""
